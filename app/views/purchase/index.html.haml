.main-container
  .purchase
    .purchase__confirmation
      購入を確定しますか？
    .purchase__goods
      .purchase__goods--name
        = @item.name
      .purchase__goods--price
        = @item.price
        円（税込）
  .creditcard
    .creditcard__title
      支払い方法
    .creditcard__content
      カード番号
      = "**** **** **** " + @default_card_information.last4
    .creditcard__content
      有効期限
      - exp_month = @default_card_information.exp_month.to_s
      - exp_year = @default_card_information.exp_year.to_s.slice(2,3)
      = exp_month + " / " + exp_year
    = link_to "購入する", pay_purchase_path, method: :post, class: "creditcard__content--purcharce"
   